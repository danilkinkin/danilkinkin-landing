# Danilkinkin site

## Development

```bash
# Install dependencies
deno install --env-file

# Add new dependency
deno install --env-file npm:@some-dependency
```

```bash
# Run dev server on http://localhost:3000
deno run dev
```
